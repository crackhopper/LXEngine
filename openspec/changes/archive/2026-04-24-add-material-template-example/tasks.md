## 1. Example Asset

- [x] 1.1 Create one repository-owned non-`blinnphong_0` material template example.
- [x] 1.2 Ensure the example loads through the current `.material` path rather than a one-off code path.

## 2. Consumer Coverage

- [x] 2.1 Add a focused consumer path in demo/test/docs that proves the example is usable.
- [x] 2.2 Validate the example against the current canonical material parameter contract.

## 3. Verification

- [x] 3.1 Build and run the focused material loader/render path that consumes the example.
- [x] 3.2 Update notes/tutorials to reference the concrete example asset.

# Planned Requirements

本目录记录已经讨论清楚、但当前不进入 active 实施批次的技术路线。

`planned/` 与 `pending/` 的区别：

- `pending/`：后续候选需求，通常仍偏产品/能力层面的待排期事项。
- `planned/`：路线已经值得保留，重点记录原理、数据流、实现步骤和边界，但当前明确不实现。

当前 planned 路线：

| 计划 | 主题 |
|---|---|
| `bake-reflection-probe-plan.md` | Reflection Probe Bake |
| `bake-irradiance-probe-sh-plan.md` | Irradiance Probe / SH Bake |
| `bake-lightmap-plan.md` | Lightmap Bake |
